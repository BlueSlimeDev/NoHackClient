package dev.mruniverse.nohackclient.listeners.bans;

import dev.mruniverse.nohackclient.NoHackClient;

public class BanController {
    private final NoHackClient plugin;

    public BanController(NoHackClient plugin) {
        this.plugin = plugin;
        init();
    }

    private void init() {
        /*
         * if(dupe) == (dupeBanC))
         */
    }
}
